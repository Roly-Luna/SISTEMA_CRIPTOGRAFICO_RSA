#pragma once
#include <iostream>
#include <vector>
#include <string>
#include <map>
#include <cmath>
#include <cctype>

// Esta librería es clave en C++/CLI para convertir textos de la interfaz (System::String^) a textos de C++ (std::string)
#include <msclr\marshal_cppstd.h>

using namespace std;